/**
 * SalesFlow-Lite entrypoint.
 * Not yet wired for real execution — cron jobs (per docs/RECIPE.md §2.4)
 * invoke the individual sweep functions directly (runDiscoverySweep,
 * runResearchPass, runPipelineSweep) rather than this file in production;
 * this remains as a local dev/manual-run convenience once implemented.
 */

export {};
